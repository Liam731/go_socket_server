# Redis configuration
REDIS_ADDRESS=your_redis_address  # e.g., 123.45.67.89:6379 or use the hostname of your Redis server
REDIS_PASSWORD=your_redis_password  # Leave empty if there's no password
REDIS_DB=0  # The Redis database number, typically between 0-15

# Server configuration
SERVER_ADDRESS=your_server_address  # e.g., :8080, the port the server will bind to
